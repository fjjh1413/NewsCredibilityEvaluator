# 2026-09-24 发布验证记录

本次整理状态工作流、RAG 证据完整性、弃权语义、报告事务边界及评测材料，并复核公开仓库的敏感信息边界。

## 发布范围与脱敏

- 不发布本机 `.env`、个人工具配置、密钥文件、运行数据库、报告、日志、备份和验证中间产物；保留环境变量示例及数据库迁移。
- 文档源码链接改为仓库相对链接；辅助文档脚本改用仓库内 `.artifacts/docs/` 或显式配置，不依赖开发者个人目录。
- 数据库示例口令改为占位符；章节演示脚本要求显式提供 `CHAPTER5_DEMO_PASSWORD`。
- 保留共享契约、前端构建配置源码、测试依赖的 Trace 样本及具有来源说明的冻结评测数据。
- 当前源码和 Git 历史分别检查。历史凭据清理不等于凭据失效；已公开过的凭据须由账户持有人在供应商后台撤销。

## 本次实际验证

| 检查 | 结果 | 复现方式 |
|---|---|---|
| 后端完整测试 | 565 项通过 | 在 `backend/` 执行 `python -m unittest discover -s tests -p "test_*.py"` |
| 评测工具完整测试 | 146 项通过，4 项已有 sklearn fixture 警告 | `python -m pytest evaluation/tests -q` |
| 前端测试 | 26 项通过，无失败或跳过 | 在 `frontend/` 执行 `node --test` |
| 独立目录打包与启动 | API/worker 导入、共享契约、干净依赖安装、前端生产构建及 `/api/health`、`/api/ready` 通过 | `scripts/ci/check_clean_package.py`，不读取本机 `.env` |

测试禁用开发者 dotenv 和真实外部模型、Embedding、搜索服务，使用隔离数据库和测试配置。测试通过数不代表模型准确率、检索质量或生产性能。

本机日志保存在未跟踪的 `.artifacts/publish/`。公开仓库保留测试与复现脚本；远端 CI 的实际状态应以 GitHub Actions 为准。Docker 构建、完整容器启动和生产 MySQL 部署不作为本次本机已通过结论。

## 历史记录与实现边界

此前的审计及面试手册是对应日期的快照，其中“尚未提交”等文字描述当时状态；实际发布版本以 GitHub 提交记录为准。2026-09-19 的完整 P0 实施细节见 [验收记录](../p0-implementation-2026-09-19.md)。

默认 RAG 索引仍为 v1；混合召回实现位于 v2 路径。工作流由代码定义路由，并非模型自主规划 Agent。项目四级风险人工复核及应用检索、模型效果评测仍待完成。
