# 09_phase4_user_frontend_prompts.md

> 项目：智闻辨真：基于 RAG 与大语言模型的新闻可信度评估系统  
> 角色分工：ChatGPT 负责项目规划、需求拆解、技术方案、Prompt 设计、代码审查意见；Codex 负责辅助编码、修复 Bug、补接口、补页面和生成测试。  
> 使用原则：每次只让 Codex 完成一个明确任务，完成后必须说明修改文件、运行方式、测试方式和未完成风险。


# 第四阶段：用户端 Vue 前端开发方案

## 一、阶段目标

第四阶段目标是完成普通用户可以使用的前端主流程。用户应能够完成注册、登录、提交新闻检测、查看检测结果、下载报告入口、查看历史记录、浏览高风险新闻和个人中心。

本阶段的关键不是页面数量堆叠，而是把“新闻检测结果页”做成系统演示核心页面。

## 二、技术方案

### 2.1 前端技术栈

```text
Vue 3
Vue Router
Pinia
Axios
Element Plus
ECharts
```

### 2.2 用户端页面

```text
/                 首页
/login            登录页
/register         注册页
/detect           新闻检测页
/result/:id       检测结果页
/history          历史记录页
/high-risk        高风险新闻展示页
/profile          个人中心
```

### 2.3 前端目录建议

```text
frontend/src/
├── api/
│   ├── auth.js
│   ├── detect.js
│   ├── highRisk.js
│   └── report.js
├── assets/
├── components/
│   ├── RiskLevelTag.vue
│   ├── ScoreCard.vue
│   ├── EvidenceList.vue
│   ├── AgentSteps.vue
│   └── PageHeader.vue
├── router/
│   └── index.js
├── stores/
│   └── user.js
├── utils/
│   ├── request.js
│   └── auth.js
├── views/
│   ├── HomeView.vue
│   ├── LoginView.vue
│   ├── RegisterView.vue
│   ├── DetectView.vue
│   ├── ResultView.vue
│   ├── HistoryView.vue
│   ├── HighRiskView.vue
│   └── ProfileView.vue
└── App.vue
```

### 2.4 UI 风格

采用“科技蓝白 + 安全风控平台 + 企业级 AI 平台”风格：

- 主色：蓝色、青色、白色；
- 页面留白充足；
- 检测结果用卡片分区；
- 风险等级用标签色区分；
- 证据列表清晰展示；
- 不做花哨动画，强调专业和可信。

## 三、本阶段开发提示词 1：搭建 Vue 用户端基础结构

```text
你现在负责开发“智闻辨真”项目第四阶段的用户端前端。请先阅读 docs/01_project_design.md、docs/03_api_design.md、docs/05_project_structure.md。

本次任务只搭建用户端 Vue 基础结构，不开发管理员后台。

技术栈：
Vue3 + Vue Router + Pinia + Axios + Element Plus

请完成：
1. 创建用户端基础路由；
2. 创建首页、登录页、注册页、新闻检测页、检测结果页、历史记录页、高风险新闻页、个人中心页的空页面；
3. 配置 Axios 请求实例；
4. 配置 Token 请求拦截器；
5. 配置 Pinia 用户状态；
6. 配置基础布局组件；
7. 不要调用不存在的接口写死假逻辑。

页面路由：
/、/login、/register、/detect、/result/:id、/history、/high-risk、/profile

完成后请说明：
1. 前端目录结构；
2. 启动命令；
3. 路由配置；
4. Token 保存位置；
5. 如何测试页面跳转。
```

## 四、本阶段开发提示词 2：实现登录注册页面

```text
请实现用户登录和注册页面，并对接后端接口。

接口：
POST /api/auth/register
POST /api/auth/login
GET /api/auth/me

要求：
1. 使用 Element Plus 表单；
2. 登录成功后保存 JWT Token；
3. 登录成功后获取当前用户信息；
4. 根据用户 role 判断是否普通用户或管理员；
5. 普通用户登录后跳转 /detect；
6. 管理员登录后可提示进入后台，但本阶段不开发后台页面；
7. 表单要有基本校验；
8. 登录失败时显示后端错误信息；
9. 不要把 Token 写死。

完成后请说明：
1. 修改了哪些文件；
2. 如何测试注册；
3. 如何测试登录；
4. Token 是否会自动带到请求头。
```

## 五、本阶段开发提示词 3：实现新闻检测页面

```text
请实现新闻检测页 /detect。

要求：
1. 页面包含新闻标题输入框；
2. 页面包含新闻正文输入框；
3. 可选新闻类别选择框；
4. 提交按钮调用 POST /api/detect/news；
5. 提交时显示 loading；
6. 检测成功后跳转 /result/:id；
7. 检测失败时显示错误提示；
8. 页面有系统免责声明提示：本系统为辅助评估工具，不能替代人工事实核查；
9. 可以提供“示例新闻填充”按钮，用于演示；
10. 不要在前端自己计算评分，评分以后端返回为准。

完成后请给出：
1. 页面功能说明；
2. 接口调用说明；
3. 演示用测试数据；
4. 常见错误处理。
```

## 六、本阶段开发提示词 4：实现检测结果页

```text
请实现检测结果页 /result/:id，这是系统用户端最重要的演示页面。

接口：
GET /api/detect/{id}

页面必须展示：
1. 新闻标题；
2. 检测时间；
3. final_score 可信度评分；
4. risk_level 风险等级；
5. judgement_result 判断结论；
6. reason 判断理由；
7. risk_points 风险点；
8. keywords 关键词；
9. evidence_list 检索证据 Top10；
10. similar_news 相似新闻；
11. suggestion 辟谣建议；
12. agent_steps AI 分析过程；
13. disclaimer 免责声明；
14. PDF 报告下载按钮。

组件建议：
- ScoreCard.vue
- RiskLevelTag.vue
- EvidenceList.vue
- AgentSteps.vue

UI 要求：
1. 科技蓝白风；
2. 卡片式布局；
3. 风险等级醒目；
4. 证据列表清晰；
5. 不要一整页纯文字堆叠；
6. 下载报告按钮放在明显位置。

注意：
- 不要在本页面调用 DeepSeek；
- 本页面只读取后端检测详情；
- 如果报告暂时未生成，按钮显示“报告生成中”或隐藏。

完成后请说明页面结构和接口字段映射。
```

## 七、本阶段开发提示词 5：实现历史记录页面

```text
请实现历史记录页 /history。

接口：
GET /api/detect/history

要求：
1. 使用 Element Plus 表格展示历史记录；
2. 字段包括：新闻标题、检测时间、可信度评分、风险等级、是否高风险、报告下载、查看详情；
3. 支持按风险等级筛选；
4. 支持关键词搜索；
5. 支持分页；
6. 点击查看详情跳转 /result/:id；
7. 普通用户只展示自己的记录；
8. 不要调用管理员接口。

完成后请说明：
1. 表格字段；
2. 筛选逻辑；
3. 分页逻辑；
4. 如何测试。
```

## 八、本阶段开发提示词 6：实现高风险新闻展示页与个人中心

```text
请实现高风险新闻展示页 /high-risk 和个人中心 /profile。

高风险新闻页要求：
1. 调用 GET /api/high-risk；
2. 展示最新高风险新闻；
3. 展示高风险新闻排行榜；
4. 展示高频风险关键词；
5. 展示高风险类别分布；
6. 敏感内容只展示摘要，不展示过长正文。

个人中心要求：
1. 展示当前用户基本信息；
2. 展示检测次数；
3. 展示高风险检测数量；
4. 展示报告数量；
5. 提供退出登录按钮。

注意：
- 没有接口时，不要编造复杂假数据；
- 可以先做接口占位和空状态展示；
- 样式保持与用户端一致。

完成后请说明哪些数据来自真实接口，哪些是待后端补充。
```

## 九、本阶段验收标准

```text
1. 用户可以注册登录；
2. 登录后 Token 自动带入请求头；
3. 用户可以提交新闻检测；
4. 检测成功后能进入结果页；
5. 结果页能完整展示后端返回字段；
6. 历史记录页能查看检测历史；
7. 高风险新闻页有基本展示；
8. 个人中心能显示用户信息；
9. 刷新页面后登录状态不丢失；
10. 页面风格统一。
```

## 十、代码审查提示词

```text
请作为前端代码审查员，检查第四阶段用户端 Vue 代码。

重点检查：
1. 是否把后端返回字段写错；
2. 是否在前端私自计算可信度评分；
3. Token 是否正确保存和携带；
4. 未登录是否能访问需要登录的页面；
5. 检测结果页是否存在字段为空导致报错；
6. 是否存在大量硬编码假数据；
7. 页面是否过于杂乱，不适合答辩展示；
8. 是否影响后续管理员后台开发。

请输出审查报告，不要直接改代码。
```
