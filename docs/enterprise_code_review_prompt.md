你现在是一名资深企业级全栈架构师、后端代码审查工程师、DevOps工程师和安全审计工程师。

请对当前项目进行一次“企业级项目标准”的完整代码审查。

项目名称：
智闻辨真：基于 RAG 与大语言模型的新闻可信度评估系统

技术栈：
- 后端：FastAPI
- 前端：Vue3 + Element Plus
- 数据库：MySQL
- 向量数据库：Chroma
- 大模型：DeepSeek
- 核心功能：注册登录、新闻检测、RAG 检索、可信度评分、PDF 报告、管理员后台、知识库管理、Prompt 模板管理、高风险新闻管理、数据统计图表

我的目标：
这是我的第一个希望写进简历的企业级项目。请不要只检查能不能运行，而是从“真实企业项目”的角度检查它是否具备：
1. 清晰的项目结构
2. 稳定的接口设计
3. 规范的代码分层
4. 安全的权限控制
5. 完整的异常处理
6. 可维护的配置管理
7. 可测试性
8. 可部署性
9. 可扩展性
10. 适合作为简历项目展示的工程质量

重要限制：
1. 本轮只做代码审查，不要修改任何文件。
2. 不要直接大规模重构。
3. 不要为了“企业级”盲目引入微服务、Kubernetes、复杂消息队列等过度设计。
4. 不要删除现有功能。
5. 不要修改数据库结构，除非明确说明原因、影响范围和迁移方案。
6. 不要泄露、打印、复述任何 API Key、数据库密码、SECRET_KEY 等敏感信息。
7. 如果发现敏感信息，只说明风险，不要输出完整密钥。
8. 请优先给出适合当前项目阶段的、可逐步落地的改进建议。

请重点审查以下方面：

一、整体架构审查

请检查：
1. 后端目录结构是否符合 FastAPI 企业级项目习惯。
2. router、schema、service、crud、model、core、utils 分层是否清晰。
3. 是否存在业务逻辑写在 router 里的问题。
4. 是否存在数据库操作写在前端或接口层的问题。
5. 是否存在 service 层职责过重、函数过长、逻辑混杂的问题。
6. 是否存在重复代码。
7. 是否存在命名混乱、模块边界不清晰的问题。
8. 前端 views、api、utils、components 是否分层合理。
9. 管理员端和普通用户端逻辑是否清晰隔离。
10. 当前架构是否适合继续扩展 RAG、Agent、统计分析、报告管理等功能。

二、后端企业级规范审查

请重点检查：
1. FastAPI 路由设计是否规范。
2. API 路径命名是否统一。
3. HTTP 方法使用是否合理。
4. 请求参数、响应结构是否稳定。
5. Pydantic Schema 是否完整。
6. 是否存在接口返回格式不统一的问题。
7. 是否有统一响应包装。
8. 是否有统一异常处理。
9. 是否有合理的状态码。
10. 是否存在 print 调试信息。
11. 日志是否规范。
12. 是否存在硬编码路径、硬编码阈值、硬编码密钥。
13. 配置是否集中在 config.py 或环境变量中。
14. 是否有 .env.example。
15. 是否区分开发环境、测试环境、生产环境配置。
16. 是否有数据库连接池或合理的 session 生命周期管理。
17. SQLAlchemy 使用是否规范。
18. 是否存在事务不一致风险。
19. 是否存在 N+1 查询或明显低效查询。
20. 是否有必要的分页、筛选、排序。

三、数据库与数据一致性审查

请检查：
1. MySQL 表设计是否合理。
2. 主键、外键、索引是否合理。
3. 字段命名是否规范。
4. 时间字段 created_at、updated_at 是否统一。
5. 软删除或物理删除策略是否清晰。
6. 知识库 MySQL 与 Chroma 向量数据是否可能不一致。
7. 新增、修改、删除知识库时是否有同步机制。
8. 向量化失败时是否有补偿。
9. 删除 Chroma 成功但 MySQL 失败时是否有补偿。
10. 是否有数据状态字段，例如 vector_sync_status。
11. 是否有初始化数据脚本。
12. 是否有迁移方案或建表说明。
13. 是否适合答辩演示和后续部署。

四、RAG 与大模型调用审查

请检查：
1. RAG 检索流程是否清晰。
2. query、top_k、category 等参数是否合理。
3. Chroma 查询结果是否有兜底处理。
4. evidence_list 是否结构稳定。
5. Prompt 模板是否有变量校验。
6. Prompt 模板缺失、禁用、格式错误时是否有 fallback。
7. 调用 DeepSeek 前是否再次校验 Prompt。
8. 大模型返回非 JSON、空响应、异常响应时是否能处理。
9. 是否存在重复调用大模型的问题。
10. LLM 调用失败时是否会写入错误结果。
11. 是否有超时、重试、错误日志。
12. 是否会泄露 API Key。
13. scoring 逻辑是否稳定。
14. 评分公式是否集中管理。
15. 风险等级阈值是否重复定义。
16. 高风险判断是否安全兜底。
17. 当前 embedding 是否具备语义检索能力，如果只是 placeholder，请明确指出影响和改进方案。

五、安全审查

请检查：
1. 登录注册是否安全。
2. 密码是否使用 bcrypt 等安全哈希。
3. JWT SECRET_KEY 是否安全。
4. Token 过期时间是否合理。
5. 无效 token 是否返回 401。
6. 管理员权限是否严格校验。
7. 普通用户是否能越权访问管理员接口。
8. 用户是否能访问别人的检测记录。
9. PDF 下载是否存在路径穿越风险。
10. 上传或文件处理是否存在安全风险。
11. CORS 配置是否安全。
12. 是否存在 SQL 注入风险。
13. 是否存在 XSS 风险。
14. 是否存在敏感信息写入日志的问题。
15. 是否有接口频率限制需求。
16. 是否有输入长度限制。
17. 是否有危险 Prompt 注入风险。
18. 是否有大模型输出未清洗直接展示的问题。

六、前端企业级规范审查

请检查：
1. Vue3 页面结构是否清晰。
2. views、components、api、utils 是否职责清晰。
3. 是否存在重复函数。
4. 是否有统一 request 封装。
5. 是否统一处理 token。
6. 是否统一处理接口错误。
7. 是否有 loading、empty、error 状态。
8. 表单校验是否完整。
9. 路由权限是否完善。
10. 管理员页面是否有前端权限保护。
11. ECharts 初始化、resize、销毁是否规范。
12. 页面字段是否和后端响应一致。
13. sessionStorage/localStorage 使用是否合理。
14. 是否存在过期缓存问题。
15. 是否存在过度兼容字段导致维护困难。
16. UI 是否适合课程答辩截图和企业级项目展示。
17. npm run build 是否应该通过。
18. 是否有 lint 或格式化规范。

七、测试质量审查

请检查：
1. 是否有后端单元测试。
2. 是否有接口测试。
3. 是否覆盖注册、登录、检测、RAG、知识库、Prompt、报告、管理员权限。
4. 是否有异常场景测试。
5. 是否有权限越权测试。
6. 是否有高风险评分边界测试。
7. 是否有 LLM 失败模拟测试。
8. 是否有 MySQL/Chroma 一致性测试。
9. 前端是否有构建测试。
10. 是否需要补充最小可用测试清单。
11. 请给出“答辩前必须跑的测试命令”。

八、部署与交付审查

请检查：
1. 是否有 README。
2. 是否有启动说明。
3. 是否有 .env.example。
4. 是否有数据库初始化说明。
5. 是否有前端构建说明。
6. 是否有后端启动说明。
7. 是否有 Dockerfile 或 docker-compose，如果没有请说明是否必须。
8. 是否有生产部署注意事项。
9. 是否有日志目录、报告目录、上传目录说明。
10. 是否有课程答辩演示流程。
11. 是否有截图清单。
12. 是否适合写进简历。
13. 简历中可以怎么描述这个项目。
14. 面试官可能会追问哪些技术点。

九、输出格式要求

请严格按照以下格式输出审查报告：

# 企业级代码审查报告

## 一、总体结论

请给出：
1. 当前项目是否具备“企业级项目雏形”
2. 是否适合作为简历项目
3. 是否适合进入答辩演示
4. 当前工程质量等级：A / B / C / D
5. 当前主要短板
6. 最建议优先修复的 5 个问题

## 二、企业级成熟度评分

请用表格输出：

| 维度 | 分数/10 | 当前表现 | 主要问题 | 改进建议 |
|---|---:|---|---|---|
| 架构分层 |  |  |  |  |
| 后端规范 |  |  |  |  |
| 前端规范 |  |  |  |  |
| 数据库设计 |  |  |  |  |
| RAG 实现 |  |  |  |  |
| 大模型调用 |  |  |  |  |
| 安全性 |  |  |  |  |
| 测试覆盖 |  |  |  |  |
| 部署交付 |  |  |  |  |
| 简历展示价值 |  |  |  |  |

## 三、必须立即修复的问题

只列出会影响安全、数据一致性、系统稳定性、答辩演示的问题。

每个问题按以下格式：

### 问题 1：标题

- 严重等级：高
- 企业级影响：
- 涉及文件：
- 涉及函数：
- 问题描述：
- 影响后果：
- 推荐修复方案：
- 是否需要修改数据库：
- 是否影响现有功能：
- 建议优先级：P0 / P1 / P2

## 四、建议联调前修复的问题

列出中等问题，包括代码规范、可维护性、接口一致性、异常处理、日志、测试不足等。

格式同上。

## 五、可以后续优化的问题

列出低优先级问题，包括代码抽取、命名优化、文档完善、体验优化等。

## 六、前后端接口一致性检查

请用表格输出：

| 模块 | 前端请求/字段 | 后端接口/字段 | 是否一致 | 问题 | 修复建议 |
|---|---|---|---|---|---|

## 七、权限与安全风险清单

请用表格输出：

| 风险点 | 涉及位置 | 风险等级 | 企业级要求 | 修复建议 |
|---|---|---|---|---|

## 八、数据一致性风险清单

请用表格输出：

| 场景 | 当前流程 | 风险 | 推荐企业级处理方式 |
|---|---|---|---|

## 九、测试补充清单

请输出：
1. 后端必须补充的测试
2. 前端必须验证的页面
3. 答辩前必须手动跑通的流程
4. 推荐测试命令

## 十、企业级改造路线图

请按照阶段输出：

### 第一阶段：答辩前必须修复

列出 5 个以内最关键问题。

### 第二阶段：简历项目前必须优化

列出让项目更像企业级项目的重点改造。

### 第三阶段：面试加分项

列出可以作为亮点讲的优化，例如：
- 统一异常处理
- 统一响应结构
- 日志体系
- 权限控制
- RAG 可替换 embedding
- Prompt 模板校验
- PDF 报告安全下载
- MySQL 与 Chroma 一致性策略
- Docker 部署
- README 完整化

## 十一、给我的最终建议

请从面试官角度告诉我：
1. 这个项目当前最像企业级项目的地方是什么
2. 最不像企业级项目的地方是什么
3. 我应该优先补强哪些地方
4. 简历里应该突出哪些技术点
5. 面试时应该如何解释项目中的不足

## 十二、生成逐个修复任务

最后，请根据审查结果，生成“逐个修复任务清单”。

要求：
1. 每个任务只修一个问题。
2. 每个任务都可以直接复制给 Claude Code 或 Codex 执行。
3. 每个任务必须包含：
   - 修复目标
   - 涉及文件
   - 修改要求
   - 禁止事项
   - 测试方式
   - 完成后需要输出的内容
4. 不要生成一个大而全的重构任务。
5. 优先生成 P0 和 P1 任务。