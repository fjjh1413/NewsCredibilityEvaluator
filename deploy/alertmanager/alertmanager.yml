global:
  resolve_timeout: 5m

route:
  receiver: feishu
  group_by:
    - alertname
    - service
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: feishu
    webhook_configs:
      - url: http://feishu-alerts:8080/alert
        send_resolved: true
